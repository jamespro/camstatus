// data: list of approved plates
// needs to be in a format we can match / compare to, from a string. let's use an array
approved_plates = ["GHK234", "537VSY", "HOTMIX", "98765432"];
// future: fetch from file, fetch from remote server

// on click button id plateSubmit,
//

document.querySelector("#app").innerHTML = `
  <div>
    <h1>innerHTML</h1>
  </div>
`;
